---
title: gh-infra
description: Declarative GitHub infrastructure management via YAML
template: splash
hero:
  tagline: Like Terraform, but for GitHub — no state file required.
  actions:
    - text: Get Started
      link: ./introduction/getting-started/
      icon: right-arrow
    - text: View on GitHub
      link: https://github.com/babarot/gh-infra
      icon: external
      variant: minimal
---

import CodeShowcase from '../../components/CodeShowcase.astro';
import Features from '../../components/Features.astro';

export const importLeft = `<span class="hl-prompt">$</span> <span class="hl-cmd">gh infra import</span> babarot/my-project

<span class="hl-comment"># Exports current repo settings to YAML</span>`;

export const importRight = `<span class="hl-key">apiVersion:</span> gh-infra/v1
<span class="hl-key">kind:</span> Repository
<span class="hl-key">metadata:</span>
  <span class="hl-key">name:</span> my-project
  <span class="hl-key">owner:</span> babarot

<span class="hl-key">spec:</span>
  <span class="hl-key">description:</span> <span class="hl-str">"My awesome project"</span>
  <span class="hl-key">visibility:</span> public
  <span class="hl-key">features:</span>
    <span class="hl-key">wiki:</span> true
    <span class="hl-key">discussions:</span> false`;

export const planLeft = `<span class="hl-key">apiVersion:</span> gh-infra/v1
<span class="hl-key">kind:</span> Repository
<span class="hl-key">metadata:</span>
  <span class="hl-key">name:</span> my-project
  <span class="hl-key">owner:</span> babarot

<span class="hl-key">spec:</span>
  <span class="hl-key">description:</span> <span class="hl-str">"My awesome project"</span>
  <span class="hl-key">visibility:</span> public
  <span class="hl-key">features:</span>
    <span class="hl-key">wiki:</span> <span class="hl-changed">false</span>
    <span class="hl-key">discussions:</span> <span class="hl-changed">true</span>
  <span class="hl-key">topics:</span>
    - <span class="hl-changed">go</span>
    - <span class="hl-changed">cli</span>`;

export const planRight = `<span class="hl-dim"># babarot/my-project will be updated</span>
  <span class="hl-change">~</span> <span class="hl-repo">babarot/my-project</span>
      <span class="hl-change">~</span> features.wiki         <span class="hl-old">true</span> <span class="hl-dim">→</span> <span class="hl-new">false</span>
      <span class="hl-change">~</span> features.discussions  <span class="hl-old">false</span> <span class="hl-dim">→</span> <span class="hl-new">true</span>
      <span class="hl-add">+</span> topics                <span class="hl-new">[go, cli]</span>

<span class="hl-dim">━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━</span>

Plan: <span class="hl-repo">0</span> to create, <span class="hl-repo">1</span> to update, <span class="hl-repo">0</span> to destroy
To apply, run: <span class="hl-repo">gh infra apply</span>`;

<div class="section-label">
  <span class="section-label-text">Import existing repos</span>
</div>

<CodeShowcase
  leftTitle="terminal"
  rightTitle="repos/my-project.yaml"
  leftHtml={importLeft}
  rightHtml={importRight}
/>

<div class="section-label">
  <span class="section-label-text">Edit YAML. See the diff.</span>
</div>

<CodeShowcase
  leftTitle="repos/my-project.yaml"
  rightTitle="gh infra plan"
  leftHtml={planLeft}
  rightHtml={planRight}
/>

export const applyLeft = `<span class="hl-prompt">$</span> <span class="hl-cmd">gh infra apply</span> ./repos/

<span class="hl-dim">Do you want to apply these changes?</span>
  gh-infra will apply the actions
  described above.

  <span class="hl-dim">Only 'yes' will be accepted.</span>
  <span class="hl-dim">Enter a value:</span> <span class="hl-new">yes</span>`;

export const applyRight = `<span class="hl-dim">━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━</span>

  <span class="hl-new">✓</span> <span class="hl-repo">babarot/my-project</span>
      <span class="hl-new">✓</span> features            updated
      <span class="hl-new">✓</span> topics              updated

<span class="hl-dim">━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━</span>

Apply complete! 2 changes applied.`;

<div class="section-label">
  <span class="section-label-text">Apply with confidence</span>
</div>

<CodeShowcase
  leftTitle="gh infra apply"
  rightTitle="result"
  leftHtml={applyLeft}
  rightHtml={applyRight}
/>

<Features />
